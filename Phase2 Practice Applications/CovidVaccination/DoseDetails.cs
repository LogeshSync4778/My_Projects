namespace CovidVaccination
{
    /// <summary>
    /// Enum type used to represent the dose count of the beneficiary in <see cref="BeneficiaryClass" /> Class Instance
    /// </summary>
    public enum DoseDetails
    {
        select,one, two, three
    }
}